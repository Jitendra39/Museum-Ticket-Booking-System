<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Museum Ticket Booking</title>
    <style><%= cssContent %></style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/0.4.1/html2canvas.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/1.3.2/jspdf.min.js"></script>  
</head>
<body>
    <div class="container my-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card ticket-card" id="ticket">
                    <div class="card-body p-4">
                        <h2 class="card-title text-center mb-4"><%= data.museumName %></h2>
                        <div class="row">
                            <div class="col-md-8">
                                <p><strong>Address:</strong> <%= data.address %></p>
                                <p><strong>Date:</strong> <span id="bookingDate"><%= data.date %></span></p>
                                <p><strong>Visitor:</strong> <span id="visitorName"><%= data.name %></span></p>
                                <p><strong>Ticket ID:</strong> <span id="ticketId"><%= data.id %></span></p>
                                <p><strong>Price:</strong> <%= data.price %></p>
                            </div>
                            <div class="col-md-4 text-center">
                                <img src="<%= data.Qr %>" alt="QR Code" class="img-fluid qr-code mb-2" aria-label="Scannable QR code for ticket validation">
                                <small class="text-muted">Scan for validation</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
      const data = <%- JSON.stringify(data) %>;
      console.log(data);
    </script>
</body>
</html>